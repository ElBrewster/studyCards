generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model NoteCard {
  id        Int     @id @default(autoincrement())
  question  String
  title     String
  answer    String
  seriesNum Int?
  deck      String?
}
